name: merchant_integration_pg
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.8.0
  google_fonts: ^6.2.1
  encrypt: ^5.0.3
  pointycastle: ^3.9.1
  http: ^1.2.2
  intl: ^0.19.0
  url_launcher: ^6.3.0
  flutter_dotenv: ^5.1.0
  path_provider: ^2.1.4
  file_selector: ^1.0.3
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/images/
    - assets/images/LogoMain.jpg
    - assets/itemImages/